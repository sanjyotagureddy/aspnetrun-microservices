# EventBus.Messages

Brief: Shared messaging/events library used by microservices for RabbitMQ integration.

Usage: Reference this project from services that publish/subscribe to events.

No runtime to run; build as part of solution.

Build:
```bash
cd src/BuildingBlocks/EventBus.Messages
dotnet build
```
